#include <iostream>
using namespace std;

const int maxn = 10010;
bool dp[maxn];
int a[110];

int gcd(int x, int y) { return y == 0 ? x : gcd(y, x % y); }

int main() {
  int n;
  scanf("%d", &n);

  return 0;
}